import {
    login
  } from "@/api/user"
  
  const state = {
  }
  const mutations = {
    "INIT_DATA": function (state) {
    },
    "SET_DATA": function (state, userInfo) {
    }
  }
  const getters = {
  }
  const actions = {
  }
  export default {
    namespaced: true,
    state,
    mutations,
    getters,
    actions
  }
  